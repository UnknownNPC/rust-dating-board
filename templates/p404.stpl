<% use rust_i18n::t; %>

<!DOCTYPE html>
<html lang="en">

<head>
    <% include!("./includes/head_body.stpl"); %>
</head>

<body>
    <% let active_tab = ""; %>
    <% include!("./includes/nav.stpl"); %>

        <div class="container">
            <h2 class="text-center pt-3"><%= t!("404_error") %></h2>
        </div>
    <% include!("./includes/footer.stpl"); %>
</body>
<% include!("./includes/extra_scripts.stpl"); %>

</html>